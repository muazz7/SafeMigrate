# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# local tooling config
.claude/
MEMORY.md

# internal working documents — kept local, not published
CLAUDE.md
BUILD-SPEC-v3.md

# debug
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# env files — never commit real keys
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# --- Android (BUILD-SPEC §13.2, §16.6) ---
# The keystore and its passwords must NEVER be committed.
*.keystore
*.jks
android/keystore.properties
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/
# Synced web assets — regenerated by `npx cap sync`.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# --- Privacy (BUILD-SPEC §16.6) ---
# Only redacted documents belong in the repo.
samples/originals/
