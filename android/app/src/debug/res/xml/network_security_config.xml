<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG BUILDS ONLY — this file lives in src/debug and is never merged into a
    release APK.

    Permits cleartext to the emulator's host loopback (10.0.2.2) so `next dev` can
    be exercised from the WebView during development. The release build keeps
    android:usesCleartextTraffic="false" from the main manifest.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
